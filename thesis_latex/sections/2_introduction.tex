\section{Introduction}
\label{sec:introduction}

Deep neural networks learn powerful representations from static, independent, and identically distributed data, but a vulnerability appears as soon as training becomes sequential: the network overwrites previously acquired knowledge to accommodate new data. This phenomenon, catastrophic forgetting \cite{mccloskey:catastrophic, ratclif1990catastrophicforgetting}, is a major hurdle in machine learning. Retraining from scratch solves the issue, but it is too expensive for systems that need to adapt continuously. Continual learning (CL) studies how to learn from a sequence of tasks without that cost, and its core difficulty is the stability--plasticity dilemma: the model must stay plastic enough to learn new distributions while staying stable enough to preserve old ones \cite{Mermillod2013Stability-Plasticity}.

Replay, the family this thesis studies, stores a small buffer of past examples and trains on them alongside the new data. Replay methods reduce long-term forgetting well, but until recently they were evaluated only at task boundaries, which hides what happens within a task. \citet{delange2023continualevaluationlifelonglearning} evaluated the network after every update and identified the \emph{stability gap}: a severe but transient collapse in past-task accuracy that occurs immediately when a new task begins, followed by a slow recovery. This matters for two reasons. A deployed system is queried throughout training, so a model caught mid-transition commits errors its final checkpoint never would. And because the model eventually recovers, past knowledge is disrupted rather than lost, which means the disruption itself is the target for repair.

The discovery raised a natural question: would a better approximation of the joint loss over all tasks remove the gap? \citet{hess2024complementaryperspectivescontinuallearning} showed that the gap persists even under incremental joint training, where the model has access to all data from all tasks at every step. A perfect objective still produces transient forgetting. The problem therefore lies in \emph{how} the optimisation proceeds, and this thesis locates it on two levels: the descent path itself is bent, and real optimisers overshoot whatever path they follow.

\begin{figure}[htbp]
    \centering
    \includegraphics[width=\linewidth]{img/loss_landscape_visualization_3.png}
    \caption{\textbf{2D optimisation landscape at a task transition.} Single-task losses \textbf{Task A} (red) and \textbf{Task B} (blue) are minimised at $A$ and $B$; the \textbf{joint loss} (purple) is minimised at $A\cap B$. The steepest-descent trajectory (green curve) illustrates the trajectory problem (Section~\ref{subsec:trajectory_problem}), bowing outside A's basin into regions of higher loss. Conversely, the dashed black chord sketches the monotone reference path of Section~\ref{subsec:reference}, which stays inside the basin. Overshoot drivers (Section~\ref{subsec:trajectory_problem}) further push the iterate off these paths.}
    \label{fig:loss_landscape_visualization}
\end{figure}

\paragraph{Two levels.}
Figure~\ref{fig:loss_landscape_visualization} visualises the transition as a switch from a learned task (Task A) to a new task (Task B), a conceptual reduction of a high-dimensional surface using the loss-landscape lens of \citet{kao2021naturalcontinuallearningsuccess}. \emph{Level one} is geometric. Starting at the converged minimum $A$, steepest descent on the joint loss follows the green curve: because the steepest direction differs from the direction toward the joint minimum $A\cap B$ whenever curvature is anisotropic, the trajectory bows out of Task A's basin and returns from outside \citep{kao2021naturalcontinuallearningsuccess}, so the Task-A loss rises above its final level and comes back. This arc is deterministic, it appears with exact gradients and vanishing step size, and its signature is a hanging tail. This increase is an artifact of the specific trajectory rather than an unavoidable cost. Reaching the joint optimum requires only that the past-task loss monotonically rise to its higher joint value. Section~\ref{subsec:reference} constructs a reference path along which the loss does exactly this, sketched as the dashed chord in Figure~\ref{fig:loss_landscape_visualization}, and the mode-connectivity literature confirms that such low-loss routes exist in practice \citep{garipov2018losssurfacesmodeconnectivity, mirzadeh2020linearmodeconnectivitymultitask}. Everything the accuracy curve does below its settled level and back is therefore avoidable in principle. \emph{Level two} is that real optimisers overshoot whatever path they follow. At the boundary the objective switches in one step from the replay loss to the joint loss, so the target teleports from $\theta_A^*$ to $\theta_{\text{joint}}^*$ while the parameters stand still, leaving the iterate maximally imbalanced, replay gradient near zero, new-task gradient largest \citep{delange2023continualevaluationlifelonglearning}. A finite step on that imbalanced push carries the iterate far out of the old basin, and finite-buffer gradient noise \citep{aljundi2019gradientbasedsampleselection} widens and prolongs the excursion. This overshoot acts on the bowed path and the reference alike. Section~\ref{sec:motivation} develops both levels.

\paragraph{One tool: gate the new-task gradient.}
Sequential replay methods share a single update, a weighted combination of the current-task and replay gradients \citep{sodhani2022introductionlifelongsupervisedlearning}. Section~\ref{subsec:gates} generalises this: every replay method applies a \emph{gate} to the new-task gradient and lets the replay gradient restore at full strength, $d = G\, g_{\text{cur}} + g_{\text{rep}}$. The gate's \emph{domain} can be scalar (attenuate the whole push) or directional (attenuate per direction), and its \emph{control} feedback (react to a live signal) or feedforward (commit to a schedule or precomputed filter). This thesis builds and compares the two pure designs. The first is the \textbf{$\lambda$-curriculum}, a scalar gate. Instead of switching abruptly to the joint loss, it ramps the new-task weight from $0$ to $1$ over $N$ steps,
\begin{equation}
    \begin{aligned}
        L_\lambda(t) & = L_{\text{replay}} + \lambda(t)\, L_{\text{current}}, \\
    \end{aligned}
    \label{eq:lambda_curriculum}
\end{equation}
so the minimiser of the objective moves along a continuous path from $\theta_A^*$ to $\theta_{\text{joint}}^*$ and the model tracks a moving valley floor. Its adaptive variant sets $\lambda$ from the ratio of the replay and new-task gradient norms, a live damage signal, which makes it a scalar \emph{feedback} gate. The second is \textbf{asymmetric preconditioned experience replay} (asymmetric PER), a directional \emph{feedforward} gate. It filters the new-task gradient through the curvature of the replay loss,
\begin{equation}
    d \;=\; \delta\,(F_{\text{rep}} + \delta I)^{-1} g_{\text{cur}} \;+\; g_{\text{rep}},
    \label{eq:asym_per}
\end{equation}
where $F_{\text{rep}}$ is the replay Fisher information and $\delta$ sets the filter strength. The push passes at full gain where the past task is flat and is braked where the past task is steep, while the restoring replay gradient stays untouched.

\paragraph{Contributions.} This thesis makes four contributions.
\begin{enumerate}
    \item \textbf{A two-level account of the stability gap.} The gap separates into a deterministic arc of the descent path and overshoot of the followed path, with the permanent stability--plasticity cost split off by a monotone reference path (Section~\ref{sec:motivation}). Controlled experiments confirm the separation: removing the overshoot drivers leaves the arc, fixing the path leaves the overshoot, and doing both removes the gap.
    \item \textbf{A unified gate view of replay methods}, which places existing methods and the two designs of this thesis in one two-axis taxonomy and turns the taxonomy's predicted failure modes into testable hypotheses (Section~\ref{subsec:gates}).
    \item \textbf{The $\lambda$-curriculum}, a scalar feedback gate with an $O(1/N)$ tracking guarantee, an adaptive schedule, a $\lambda_{\min}$ plasticity floor, and a characterisation of its interplay with momentum.
    \item \textbf{Asymmetric PER}, a directional feedforward gate, with a damping sweep that maps its stability--plasticity trade-off and identifies its failure modes: an unexpected late collapse, consistent with blind spots of the sampled curvature metric, and the unravelling of per-step protection under momentum.
\end{enumerate}

% Section~\ref{sec:literature} reviews continual learning, experience replay, the stability gap, and the unified update that underlies the gate view. Section~\ref{sec:motivation} develops the two-level account and the gate taxonomy. Section~\ref{sec:theory} constructs the monotone reference path and proves the tracking bound. Section~\ref{sec:proposed_approach} turns the account into four research questions, and Section~\ref{sec:methods} describes the testbeds, metrics, and conditions that answer them. Section~\ref{sec:results} reports the experiments on rotated MNIST and CIFAR-10, and Section~\ref{sec:discussion} weighs the evidence and states the limitations.
